/**
 * 滑动窗口做题模版
 */

let left = 0 , right = 0

const fn = s => {
    while (right < s.length) {
        // ...
        right++
        // while ( need shirking ) {
        //     left++
        // }
    }
}
